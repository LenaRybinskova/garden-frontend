---
version: null
meta:
  environment: node
  toolchain:
    name: npm
    version: 20.18.3
build:
  skip: false
  additionalCommands: npm install && npm run build
  artifacts:
    '.next': .next
run:
  command: npm run start
  persistenceMount: /data
  containerPort: 3000